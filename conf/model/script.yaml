defaults:
  - base_script
  - _self_

name: student_script
source: script
# "module:function" -> callable(cfg) returning list[Path] of produced weights.
entrypoint: feral_segmentor.models.example_export:export
weights_dir: models/checkpoints/student_script
